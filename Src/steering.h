/**
  * Steering position controller — public API declared in util.h.
  * Implementation in Src/steering.c uses flat globals (steer_* vars),
  * steerCtrl_* functions, and a private SteerState enum.
  * main.c calls steerCtrl_Init/steerCtrl_StartHoming from util.h.
*/

#ifndef STEERING_H
#define STEERING_H

#include <stdint.h>

#endif // STEERING_H
